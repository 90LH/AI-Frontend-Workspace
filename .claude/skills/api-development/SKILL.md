---
name: api-development
description: 前端 API 接口开发规范 — 定义类型、使用已有 HTTP 实例、处理状态、验证
---

# api-development

> 完整执行规则见 [`core/ai/skills/api-development.md`](../../../core/ai/skills/api-development.md)。
> 本文件为 Claude Code slash command 入口，不重复维护完整逻辑。

## 触发条件

- 对接新的后端接口
- 修改已有 API 请求参数或响应处理
- 新增 API 模块（如新业务域的接口文件）

## 输出格式

见 `core/ai/skills/api-development.md` 输出格式章节。
